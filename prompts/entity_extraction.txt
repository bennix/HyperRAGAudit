You are an information extraction specialist. Your task is to extract named entities and their relationships from the following document text.

ENTITY TYPES to extract:
- Person: names of individuals
- Company: company or organisation names
- Amount: monetary values with currency
- Date: dates, time periods, deadlines
- Regulation: laws, standards, regulations, clauses
- Document: contract names, invoice numbers, report titles
- Location: addresses, cities, countries

RELATION TYPES to extract:
- signed_by: a document/contract signed by a person
- issued_by: a document issued by a company
- paid_to: an amount paid to a company/person
- dated: a document/entity associated with a date
- regulated_by: an entity governed by a regulation
- located_at: an entity associated with a location
- references: one document referencing another
- amounts_to: a transaction/item with a monetary value

RULES:
1. Only extract entities and relations that are explicitly stated in the text.
2. Each entity must include its exact name as it appears in the text.
3. Each relation must reference entities by their exact names.
4. Output ONLY valid JSON — no markdown fences, no commentary.

OUTPUT JSON SCHEMA:
{
  "entities": [
    {
      "name": "<exact text>",
      "entity_type": "<one of the types above>"
    }
  ],
  "relations": [
    {
      "source": "<entity name>",
      "target": "<entity name>",
      "relation_type": "<one of the types above>"
    }
  ]
}

DOCUMENT TEXT:
{text}